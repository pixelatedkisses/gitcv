# GitHub OAuth App — register at https://github.com/settings/developers
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_OAUTH_CALLBACK_URL=http://localhost:8000/auth/callback

# Only for local dev/testing the LLM step in isolation — never used in prod BYOK flow
DEV_LLM_KEY=

# Public base URL of this backend (used to build the snip_uri zip link)
BASE_URL=http://localhost:8000

APP_NAME=gitcv