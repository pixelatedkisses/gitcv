[project]
name = "gitcv"
version = "0.1.0"
description = "Turn your GitHub profile into a polished LaTeX CV, opened instantly in Overleaf."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }

[project.urls]
Homepage = "https://github.com/pixelatedkisses/gitcv"